<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!---->
    <declare-styleable name="ProgressView">
        <!-- 进度条半径 -->
        <attr name="p_radio" format="dimension" />
        <!-- 进度条颜色 -->
        <attr name="p_color" format="reference|color" />
        <!-- 小球半径 -->
        <attr name="titleradio" format="dimension" />
        <!-- 小球颜色 -->
        <attr name="titlecolor" format="reference|color" />

        <!-- 默认音量 -->
        <attr name="defaultVoice" format="integer" />
        <!-- 音量条宽度 -->
        <attr name="voiceBarWidth" format="dimension" />
        <!-- 音量条之间的间距 -->
        <attr name="voiceBarDistance" format="dimension" />
        <!-- 背景音量条的高度 -->
        <attr name="bgVoiceHeight" format="dimension" />
        <!-- 音量条的高度 -->
        <attr name="fgVoiceHeight" format="dimension" />

        <!-- 音量条的颜色 -->
        <attr name="fgVoiceColor" format="reference|color" />
    </declare-styleable>
    <declare-styleable name="ScanCycleView">
        <!-- 进度条半径 -->
        <attr name="s_radio" format="dimension" />
        <!-- 进度条颜色 -->
        <attr name="s_color" format="reference|color" />
        <!-- 小球半径 -->
        <attr name="s_titleradio" format="dimension" />
        <!-- 小球颜色 -->
        <attr name="s_titlecolor" format="reference|color" />
        <!--是否留一个缺口-->
        <attr name="issweep" format="boolean"></attr>
        <!--缺口角度：0~360-->
        <attr name="sweepangle" format="integer" />
        <attr name="isgradlint" format="boolean"></attr>
    </declare-styleable>

</resources>